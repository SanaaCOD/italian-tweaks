<UserControl x:Class="PurpleBoost.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="980">

  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel>
      <TextBlock Text="Paramètres" Style="{StaticResource PbTitle}" />
      <TextBlock Style="{StaticResource PbSubtitle}"
                 Margin="0,8,0,0"
                 Text="Préférences, logs et informations de version." />

      <UniformGrid Margin="0,16,0,0" Columns="2">
        <Border Style="{StaticResource PbCard}" Margin="0,0,10,10">
          <StackPanel>
            <TextBlock Text="Thème" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.Muted}" TextWrapping="Wrap" Text="{Binding ThemeText}" />
          </StackPanel>
        </Border>
        <Border Style="{StaticResource PbCard}" Margin="0,0,0,10">
          <StackPanel>
            <TextBlock Text="Langue" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.Muted}" TextWrapping="Wrap" Text="{Binding LanguageText}" />
          </StackPanel>
        </Border>
        <Border Style="{StaticResource PbCard}" Margin="0,0,10,0">
          <StackPanel>
            <TextBlock Text="Dossier logs" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.Muted}" TextWrapping="Wrap" Text="{Binding LogsFolder}" />
          </StackPanel>
        </Border>
        <Border Style="{StaticResource PbCard}">
          <StackPanel>
            <TextBlock Text="Version application" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,0" Foreground="{StaticResource Brush.Muted}" TextWrapping="Wrap" Text="{Binding AppVersion}" />
          </StackPanel>
        </Border>
      </UniformGrid>

      <Border Style="{StaticResource PbCard}" Margin="0,10,0,0">
        <StackPanel>
          <TextBlock Text="Exporter les logs" FontWeight="SemiBold" />
          <TextBlock Margin="0,8,0,0"
                     Foreground="{StaticResource Brush.Muted}"
                     TextWrapping="Wrap"
                     Text="Exporte le contenu actuel du panneau de logs vers un fichier .txt." />
          <Button Style="{StaticResource PbPrimaryButton}"
                  Margin="0,12,0,0"
                  HorizontalAlignment="Left"
                  Content="Exporter les logs"
                  Command="{Binding ExportLogsCommand}" />
        </StackPanel>
      </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>
